<%= form_for @order, :html => { multipart: true } do |f| %>
  <% if order.errors.any? %>
        <div id="error_explanation" xmlns="http://www.w3.org/1999/html">
      <h2><%= pluralize(order.errors.count, "error") %> prohibited this order from being saved:</h2>

      <ul>
      <% order.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div class="form-group">
      <label for="exampleInputFile">Anexo:</label>
      <%= f.file_field :attachment %>
    </div>

    <div class="form-group">
      <label>Preco Unitario (Em Euros):</label>
      <div class="input-group">
        <div class="input-group-addon">€</div>
        <%= f.text_field :price, placeholder: "Valor", class:"form-control" %>
      </div>
    </div>


  <div class="form-group">
    <label>Data de Entrega: </label><br>
    <%= f.date_select :deadline %>
  </div>

  <div class="form-group">
    <label>Descricao: </label><br>
    <%= f.text_area :description, size:"40" %>
  </div>

  <div class="form-group">
    <%= f.hidden_field :user_id %>
  </div>


  <div class="botao_form">
    <%= f.submit %>
  </div>
<% end %>
